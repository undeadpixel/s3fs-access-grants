{
  "name": "s3fs-access-grants",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "repository": "https://github.com/undeadpixel/s3fs-access-grants",
  "author": "Josep Arus Pous",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "postinstall": "uv sync && lefthook install",
    "check": "lefthook run pre-commit --all-files --no-stage-fixed",
    "test": "uv run pytest --cov=src --cov-report=term-missing",
    "build": "uv build",
    "changeset": "changeset",
    "changeset:add": "changeset add"
  },
  "devDependencies": {
    "@changesets/cli": "2.30.0",
    "@commitlint/cli": "19.8.1",
    "@commitlint/config-conventional": "19.8.1",
    "lefthook": "1.13.6"
  }
}
